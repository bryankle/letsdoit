import axios from 'axios';
import {
	GROUP_LOAD_TASKS
	GROUP_ADD_TASK,
	GROUP_DELETE_TASK,
	GROUP_COMPLETE_TASK
} from './types';

export function groupLoadTasks(task) {
	return function(dispatch) {
		axios.
	}
}